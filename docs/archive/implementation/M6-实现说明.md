# M6 实现说明（测试、决策文档、可复现环境）

本文档说明 **M6 里程碑**：在 M0–M5 功能已具备的前提下，补齐策划书要求的 **`docs/DECISIONS.md`**、加强 **可回归单测**、更新 **README** 与可选 **Docker Compose**，便于他人按文档跑通与理解偏差。

---

## 1. M6 交付物

| 项 | 说明 |
|----|------|
| `docs/DECISIONS.md` | Vagent 与策划书 §3 / Ragent 主链路的行为差异及原因（策划书 §9 要求）。 |
| 单测 | `LlmStreamTaskRegistryTest`（取消与越权）；`LlmSseStreamingBridgeTest`（done 回调、取消时不落库回调）。 |
| `docker-compose.yml` | 可选本地 **PostgreSQL 16 + pgvector**，与 `application.yml` 默认库名/用户对齐。 |
| README | 增加 M6 行、DECISIONS 链接、Compose 与演示步骤引用。 |

---

## 2. 推荐阅读顺序

1. `docs/DECISIONS.md`（与 §3 哪里不一致、为什么）。  
2. `LlmStreamTaskRegistry`、`LlmSseStreamingBridge` + 对应 `*Test`。  
3. README「构建与测试」「可选：Docker Compose」。

---

## 3. 与 M5 的关系

M6 **不新增业务编排能力**；后续若在 DECISIONS 中承诺「空检索不调 LLM」等，再在代码中加开关并补集成测试即可。
